# Yōsai Intel Dashboard Configuration

app:
  debug: true
  host: "127.0.0.1"
  port: 8050
  secret_key: "dev-key-change-in-production"
  environment: "development"

database:
  type: "sqlite"
  host: "localhost" 
  port: 5432
  name: "yosai.db"
  user: "user"
  password: ""

security:
  secret_key: "dev-key-change-in-production"
  session_timeout: 3600
  cors_origins: []
